## 🚀 Deploy to somee.com via FTP

# Kích hoạt workflow khi có push
on:
  push:
    branches:
      - main

# Các công việc (jobs) sẽ được thực thi
jobs:
  deploy:
    # Chạy công việc trên runner là phiên bản mới nhất của Ubuntu
    runs-on: ubuntu-latest
    
    # Các bước (steps) trong công việc deploy
    steps:
      # 1. Checkout code
      - name: Checkout the code
        uses: actions/checkout@v2
        
      # 2. Thiết lập FTP
      - name: Setup FTP
        # Sử dụng action của bên thứ ba để triển khai qua FTP
        uses: sebastianpopp/ftp-deploy-action@v2
        with:
          # Các biến bí mật (secrets) được sử dụng để bảo mật thông tin đăng nhập
          ftp-server: ${{ secrets.FTP_SERVER }}
          ftp-username: ${{ secrets.FTP_USERNAME }}
          ftp-password: ${{ secrets.FTP_PASSWORD }}
          # Đường dẫn thư mục local muốn deploy (từ thư mục gốc của repository)
          local-dir: ./ # Đường dẫn đến thư mục mà bạn muốn deploy
          # Đường dẫn thư mục remote trên server (đích đến)
          remote-dir: /path/to/your/remote/directory # Đường dẫn thư mục trên server của bạn

      # 3. Chạy lệnh echo thông báo triển khai
      - name: Deploy code via FTP
        run: |
          echo "Deploying to server..."
          # Bạn có thể thêm các lệnh khác ở đây nếu cần thiết
